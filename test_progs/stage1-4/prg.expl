decl
    int a, b, c;
enddecl
int main() {
decl
    int a, b, c;
enddecl
begin
    read(b);
    read(a);
    read(c);
    if (a < b) then
        if (b < c) then 
            write(c);
        else 
            write(b);
        endif;
    else
        if (a < c) then
            write(c);
        else
            write(a);
        endif;
    endif;
end
    return 0;
}
