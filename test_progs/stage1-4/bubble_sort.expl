decl
   int n,arr[50],i,j,dup, bubbleSort(int x, int y);
enddecl

int bubbleSort(int x, int y) {
decl
enddecl
begin
    n = 4;
    n = 1;
    n = 11;
    i = n + j;
    dup = i;
end
}

int main () {
begin
    read(n);
    i=0;
    while(i<n) do
        read(arr[i]);
        i = i+1;
    endwhile;
    i=0;
    while(i<n) do
        j=i;
        while(j<n) do
            if(arr[i]>arr[j]) then
                dup = arr[i];
                arr[i] = arr[j];
                arr[j] = dup;
            endif;
            j = j + 1;
        endwhile;
        i = i+1;
    endwhile;
    i=0;
    write("Sorted");
    while(i<n) do
        write(arr[i]);
        i = i+1;
    endwhile;
end
}
